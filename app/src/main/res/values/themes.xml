<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- ⚡ Base theme for Cookify app -->
    <style name="Base.Theme.Cookify" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Inherits from AppCompat's DayNight theme without the default ActionBar -->
        <!-- You can define global colors, fonts, and styles here -->
    </style>

    <!-- 🎨 Main app theme -->
    <style name="Theme.Cookify" parent="Base.Theme.Cookify" />
    <!-- This is the theme applied in AndroidManifest.xml -->
    <!-- Inherits everything from Base.Theme.Cookify -->

    <!-- Alias to satisfy references to @style/Cookify -->
    <style name="Cookify" parent="Theme.Cookify" />


    <!-- Centered search field style with no icon -->
    <style name="Cookify.SearchField">
        <item name="android:background">@drawable/bg_edittext</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">#000000</item>
        <item name="android:textSize">16sp</item>
    </style>
</resources>
